import { Router } from "express";

const router = Router();

// Local authentication route
router.post("/register", ()=> {});
router.post("/login", () => { });
router.get("/logout", () => { });

export default router;
